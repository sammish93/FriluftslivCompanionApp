package no.hiof.friluftslivcompanionapp.domain

class ExampleUseCase {
    // Example class that can fulfil the requirements of a user case
    // e.g. fetch api data from 2 apis, combine, and return a single instance of a class.
}