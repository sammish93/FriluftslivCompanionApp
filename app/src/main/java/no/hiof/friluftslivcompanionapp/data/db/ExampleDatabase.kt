package no.hiof.friluftslivcompanionapp.data.db

class ExampleDatabase {
    // Example of a db class. Singleton is a good pattern for a db connection, but not 100%
    // sure what's best practice with Firebase. The db class can make use out of the Firebase
    // library.
}